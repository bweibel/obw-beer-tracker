{
  "name": "obw-beer-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Ohio Brew Week beer finder SPA (Preact) + build toolchain.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "preact": "^10.24.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.0",
    "vite": "^5.4.0"
  }
}
